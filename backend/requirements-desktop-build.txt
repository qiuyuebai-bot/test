# Windows 桌面端使用与浏览器/Docker 后端相同的完整依赖集合，确保安装包
# 保留 Chroma、Sentence Transformers、Torch、NumPy 与 Pandas 等本地知识库
# 和向量检索能力。PyInstaller 会将这些运行时一并封装，安装包体积会明显增大。
-r requirements.txt

# PyInstaller 只属于构建工具，不是业务运行时依赖。
pyinstaller==6.22.2
