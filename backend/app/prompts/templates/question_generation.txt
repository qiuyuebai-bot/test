--- SYSTEM ---
你是一位专业的命题专家，擅长按难度等级命制高质量测试题与解析。
请输出严格的JSON格式，不要添加任何其他文字或markdown代码块。
--- USER ---
## 目标知识点
{knowledge_topic}

## 难度等级（1-5）
{difficulty_level}

## 本等级必须满足的命题标准
{difficulty_standard}

## 题目数量
{question_count}

## 本次变体要求
{variation_hint}

## 变体编号
{variation_seed}

## 参考知识库
{reference_knowledge}

## 本学习者近期已出现的题目（必须避免重复）
{excluded_questions}

优先基于参考知识库命题；如果参考知识库为空或资料不足，可以使用你的专业知识生成通用测试题，但题干和解析必须具体。
请生成 {question_count} 道题，并让每一道题都严格达到当前 difficulty_level 对应的命题标准。难度 1 到 5 必须体现认知要求、专业深度、推理步骤和干扰项质量的递增；不得只修改 difficulty_level 数字而保持题目内容不变。难度 4-5 的题目必须给出足够的专业条件或场景，选项都应具有迷惑性，不能通过生活常识、关键词或明显荒谬的选项直接排除。
这 {question_count} 道题必须组成同一轮连贯学习序列，围绕“{knowledge_topic}”按“基础概念与前提 → 核心机制 → 场景应用 → 边界条件与专业权衡”逐步展开；相邻题不能只是换词重复，也不能跳到无关主题。
其中必须恰好有 {multiple_choice_count} 道多选题（type 为 multiple），均匀分布在本轮中；其余为单选题（type 为 single）。多选题至少有两个正确选项，并确保只有完整选中全部正确项且不包含错误项时才成立。
每次生成都要遵循“本次变体要求”，避免与同主题历史输出使用完全相同的题干、选项、正确答案位置和解析措辞。
每题必须有 2-6 个选项、可验证的正确答案和解析。不得使用占位文字或输出任何额外字段。
解析要使用严谨措辞，避免“据说、传闻、未经证实、绝对、必然、百分百”等词；不要把普通对比词写成无来源断言。
输出 JSON。字段结构必须严格如下；下面的值只是格式示例，禁止原样复制示例内容，必须替换为围绕目标知识点的真实题目：
{{"questions": [{{"type": "single", "question": "在反向传播中，链式法则用于完成哪项计算？", "options": ["把复合函数的梯度拆成局部梯度的乘积", "把训练数据随机分成多个批次", "把连续特征转换成离散标签", "把模型参数全部初始化为零"], "correct_answers": ["A"], "explanation": "复合模型由多个函数连续组成，链式法则将损失对参数的梯度分解为各层局部梯度的乘积。", "difficulty_level": 2, "knowledge_points": ["链式法则"]}}]}}
不要输出“题干”“选项A”“正确答案”“干扰项”等字段示例或模板标签；这些词只有在解释具体判题依据时才可以作为普通表述出现。
