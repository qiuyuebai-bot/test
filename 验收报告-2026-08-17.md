# 系统验收测试报告

**项目**：领域知识个性化生成与多智能体协同决策系统
**被测版本**：53d00c1f（2026-08-17，GitHub 最新版）
**测试日期**：2026-08-17
**测试方式**：API 层（Node fetch）+ UI 层（Playwright + Edge）双通道，覆盖完整业务流程

---

## 一、对照比赛评分标准总评

| 评分维度 | 分值 | 自评 | 说明 |
|---|---|---|---|
| 作品完整性 | 30 | **22** | 核心闭环完整（4 Agent + 6 阶段流水线 + 3 种资源形态 + 可视化报告 + 2 组以上画像），但直接生成路径绕过审核、辩论修正未落库、新模块无种子数据 |
| 技术创新性 | 25 | **18** | 多智能体辩论交叉验证、SSE 实时事件流、确定性兜底策略均为亮点；但"自适应决策"存在决策与内容不一致（答对答错反馈相同），技术点展示打折 |
| 用户体验 | 15 | **9** | 页面美观、交互流畅、无 console 报错；但 Chroma 冷启动卡 86 秒、错误反馈文不对题、正确数跨会话累计、空数据页多 |
| 实用价值 | 30 | **20** | 知识溯源三级链路、隐私脱敏、审计日志完整；但幻觉率/适配准确率/覆盖率三项核心指标**均无法采集到有效数据**，无法证明达标 |
| **合计** | 100 | **69** | 骨架完整、演示可用，但三大量化指标（幻觉率<5%、适配≥85%、覆盖≥90%）目前拿不出证据 |

---

## 二、已达标项（验证通过）

| 验收要求 | 验证结果 |
|---|---|
| ≥3 个职责明确的 Agent | ✅ 4 个：DiagnosisAgent / GenerationAgent / JudgeAgent / ContentCorrector，职责独立（`backend/app/agents/`） |
| "分析-生成-校验-决策"闭环 | ✅ 6 阶段流水线：诊断→检索→生成→审核→辩论(≤3轮)→修正（[orchestrator.py:115](backend/app/agents/orchestrator.py#L115)） |
| ≥3 种资源形态 | ✅ guide / exercise / lecture（资源生成支持切换） |
| 辩论交叉验证防幻觉 | ✅ 初始审核未通过时最多 3 轮辩论，SSE 实时推送每轮进展 |
| 可视化报告 | ✅ 学情报告页：知识点覆盖率 / 难度匹配曲线 / 学习路径 |
| 自适应决策 | ✅ advance / simplify / consolidate 三分支代码与记录数据均在（decision_threshold=70%） |
| ≥2 组差异化学习者画像 | ✅ 5 组种子画像（张明-本科AI / 李雨晴-硕士AI / 王浩宇-博士AI / 陈晓-本科智能制造 / 赵静-大专工业互联网，`backend/app/data/learners.json`） |
| 隐私脱敏 / 审计日志 | ✅ 脱敏中间件 + 审计表记录（前轮已验证） |
| 知识溯源 | ✅ 资源展示→知识库片段→源文档三级链路（前轮已验证） |
| 登录/权限 | ✅ 管理员/普通用户分级，IDOR 防护校验到位 |
| UI 稳定性 | ✅ 全页面走查 console errors = 0 |

---

## 三、问题清单（按严重程度排序）

### A 类：核心功能缺陷（直接伤害验收指标）

**A1. 三大量化指标全部采不到数 —— 最致命**
- 幻觉率：`/metrics` 页面显示"样本不足"，API 采集状态长期 `collecting`，无一条有效数据
- 适配准确率：full-pipeline 生成的资源 `match_score = 0`（学情报告页"资源匹配分 0.0%"），而 `calculate_match_score`（[common.py:477](backend/app/services/common.py#L477)）**在 full-pipeline 路径根本没被调用**
- 覆盖率：报告页显示 100.0%，但数据样本明显不足，数值不可信
- **影响**：比赛要求的"幻觉率<5% / 适配准确率≥85% / 覆盖率≥90%"三项硬指标目前**零证据**，评委现场演示会直接扣分

**A2. 自适应决策名不副实：答对与答错反馈内容完全相同**
- 证据（API 实测 `GET /tutoring/history/8`）：
  - record 18：答对 → 决策 `advance`（confidence 0.95）
  - record 19：答错 → 决策 `simplify`（confidence 0.95）
  - **两条 `feedback_content` 逐字相同**（同一份"TCP 校准/减速机保养"文档）
- 决策类型变了，但给学习者的内容没有任何区别，演示"自适应"时会被一眼看穿

**A3. 反馈内容文不对题（知识库检索错配）**
- 答"机器人坐标系"题目，反馈给的是"每季度校准 TCP 精度 / 年度大保养更换减速机润滑油"——这是**设备维护**内容，不是坐标系知识
- 答"反向传播"题，进阶挑战给的是机器人维护内容（前轮已复现）
- 原因：中文语义检索质量差，向量检索命中错误片段后直接整篇拼接

**A4. 反馈内容 = 整篇文档拼接，不是精选讲解**
- `feedback_content` 直接把整份《工业机器人基础操作与维护指南》原文灌入（第一章~第三章……），未做片段筛选/归纳
- 用户读到的不是"通俗讲解"，而是原始文档全文，体验差且暴露实现粗糙

**A5. 生成题目模板化严重**
- 多次生成均为同一模板换词："理解『机器人坐标系』的**核心机制/基础概念与前提**时，哪种说明达到了基础理解要求？"（选项也是同一套 ABCD 变体）
- 本地兜底题 10 道种子（`backend/app/data/questions.json`），无 LLM 时题目雷同明显

### B 类：流程缺陷

**B1. 直接生成路径绕过审核**
- 资源页"直接生成"不走辩论流程，资源以"待审核"状态存在（新版行为），但 UI 无审核入口，资源永远停留在待审核态
- full-pipeline 生成的资源 `source_slice_ids` 为空，知识溯源字段未落库（trace 能显示是运行时数据，详情页无法回查）

**B2. Chroma 冷启动检索卡 86 秒**
- Docker 重启后首次触发知识检索，SSE 在"知识库检索"阶段静止 86 秒才继续（约等于超时边缘）
- 现场演示如果刚好是重启后第一次生成，会直接卡死观感

### C 类：用户体验细节

| # | 问题 | 位置/证据 |
|---|---|---|
| C1 | 新会话"正确数"跨会话累计：本轮未答对任何题，却显示 1/2 | reducer 从 `persisted.correctCount` 恢复（[useGuidanceSession.ts:151](src/features/guidance/useGuidanceSession.ts#L151)），与"本轮"文案矛盾 |
| C2 | 就业培训"认证"“能力评估"两个 tab 空白（无种子数据，页面只显示标题） | `/career-training/certification`、`/assessment` |
| C3 | 旧路由失效无提示：`/knowledge` 静默重定向到 dashboard | 用户收藏夹/旧链接直接进首页，找不到入口 |
| C4 | "动态自适应导学"标题下，会话难度固定为"固定基础"，无自适应迹象 | 答题后显示"本轮固定基础，继续完成剩余"，与"动态自适应"标题不符 |
| C5 | 学情报告"样本不足"提示过于简单，无引导（如何产生数据） | `/report` |

---

## 四、修复优先级建议

**P0（比赛前必须修，预计各 0.5~1 天）：**
1. A1：把 `calculate_match_score` 接入 full-pipeline 落库；幻觉率指标补一条可采集路径（或从辩论修正次数推导）
2. A2+A3：统一"决策→内容生成"链路——advance 走进阶挑战、simplify 走通俗讲解，各配独立模板，内容从**命中片段**提取而非整篇文档
3. A5：本地兜底题扩充 + 模板多样化（至少 5 套模板 × 每主题 3 变体）

**P1（演示前建议修）：**
4. B2：Chroma 冷启动预热（启动时后台预检一次检索）或加进度提示与超时兜底
5. B1：UI 增加"待审核资源"审核入口（管理员通过/驳回），或直接生成也走轻量审核
6. C1：正确数改为会话内计数

**P2（有时间再修）：**
7. C2 补种子数据、C3 旧路由加重定向提示页、C4 文案对齐

---

## 五、结论

系统**骨架完整、演示链路通畅**（登录→生成→导学→报告全流程可跑通，UI 无报错），多智能体辩论与 SSE 实时可视化是实打实的亮点。但**三大量化验收指标拿不出数据**、**自适应决策"决策与内容不一致"**这两个问题属于评委一眼可识别的硬伤，建议优先修复 A 类问题后再提交演示。
