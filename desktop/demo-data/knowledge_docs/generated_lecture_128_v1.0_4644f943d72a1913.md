# 数据集划分 专属知识讲义

**难度等级**：★★★ | **适用**：具备中等基础的开发者


## 学习目标

通过本讲义学习，你将掌握数据集划分的核心概念与应用方法，
弥补在算法设计, 系统架构, 数据分析方面的知识盲区。

---


## 1. 

**这一节解决什么**：先建立的整体地图，知道它解决什么问题。
欢迎来到本次听觉学习课程。我们将一起探索机器学习中一个基础但极其关键的环节——数据集划分。无论你是要构建预测性维护系统、优化生产排程，还是进行产品质量检测，合理的数据集划分都是模型成功的基石。本次课程专为听觉学习者设计，建议你戴上耳机，跟随我的讲解，在脑海中构建清晰的框架。
首先，我们从概念总览开始。想象一下，你是一位智能制造工厂的数据科学家，手头有一批从传感器、MES系统、ERP系统收集来的海量数据。你的目标是训练一个模型...


**核心要点**：
- 欢迎来到本次听觉学习课程。
- 我们将一起探索机器学习中一个基础但极其关键的环节——数据集划分。

**如何应用**：把和一个真实案例对照，确认它什么时候适用、什么时候不适用。
**常见误区**：最常见的问题是把和相近概念混在一起。
**自测标准**：我能把讲给别人听，并举一个例子。

## 2. 

**这一节解决什么**：先建立的整体地图，知道它解决什么问题。
接下来，我们展开关键知识点。第一个核心概念是划分比例。常见比例有70/15/15或80/10/10，但这不是绝对的。当数据量很大时，比如百万级样本，验证集和测试集可以占较小比例，例如98/1/1；当数据量很小时，比如几千条，则需要留出足够多的验证和测试数据，可能需要60/20/20。在智能制造场景中，数据往往具有时间序列特性，比如连续采集的温度、振动数据。此时，随机划分会破坏时间顺序，导致数据泄露。例如，你用前期的数据训练，用同一时间段的数据测试，模型会“记住”而不是“学习”...


**核心要点**：
- 接下来，我们展开关键知识点。
- 第一个核心概念是划分比例。
- 常见比例有70/15/15或80/10/10，但这不是绝对的。

**如何应用**：完成后自己改一个条件，看结论是否还成立。
**常见误区**：最常见的问题是把和相近概念混在一起。
**自测标准**：我能把讲给别人听，并举一个例子。

## 3. 

**这一节解决什么**：把放进真实任务里理解，确认适用边界。
第二个关键知识点是分层采样。当数据集类别不平衡时，比如故障样本仅占5%，随机划分可能导致训练集或测试集中故障样本比例过低。分层采样确保划分后的每个子集都保持原始类别比例。在Python的scikit-learn库中，`train_test_split`函数有一个`stratify`参数，可以传入标签数组实现分层。例如，`X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, strati...


**核心要点**：
- 第二个关键知识点是分层采样。
- 当数据集类别不平衡时，比如故障样本仅占5%，随机划分可能导致训练集或测试集中故障样本比例过低。
- 分层采样确保划分后的每个子集都保持原始类别比例。

**如何应用**：可以把直接转成一个小练习：先写输入，再写处理，再写输出。
**常见误区**：容易漏掉的是边界条件和例外情况。
**自测标准**：我能写出对应的一个变式题。

## 4. 

**这一节解决什么**：先抓住的关键条件，再看处理步骤。
现在，我们来深入讲解你的知识盲区。首先，算法设计。数据集划分直接影响算法设计的选择。例如，如果数据划分不当，导致验证集与训练集分布不一致，那么你在设计算法时可能会过度拟合验证集，从而在测试集上表现不佳。因此，算法设计时需考虑划分策略，比如使用嵌套交叉验证来避免选择偏差。其次，系统架构。在智能制造系统中，数据集划分通常在数据管道中实现。系统架构需要考虑数据版本控制、划分逻辑的可复用性以及数据流的一致性。例如，你可以设计一个数据预处理模块，该模块负责划分并缓存划分结果，确保模型训...


**核心要点**：
- 现在，我们来深入讲解你的知识盲区。
- 首先，算法设计。
- 数据集划分直接影响算法设计的选择。

**如何应用**：把和一个真实案例对照，确认它什么时候适用、什么时候不适用。
**常见误区**：另一个问题是只记结果，不记前提。
**自测标准**：我能说出的边界条件。

## 5. 

**这一节解决什么**：用对照和反例检验，避免只记结论。
次的样本可能同时出现在训练和测试集中，导致模型评估过于乐观。因此，需要按批次或设备ID进行分组划分。在系统架构层面，可以设计一个分组函数，确保同一组的数据不会跨集。...


**核心要点**：
- 次的样本可能同时出现在训练和测试集中，导致模型评估过于乐观。
- 因此，需要按批次或设备ID进行分组划分。
- 在系统架构层面，可以设计一个分组函数，确保同一组的数据不会跨集。

**如何应用**：把和一个真实案例对照，确认它什么时候适用、什么时候不适用。
**常见误区**：很多人会直接跳过例子，导致会看不会做。
**自测标准**：我能说明为什么这个结论成立而不是只背答案。

## 6. 

**这一节解决什么**：把放进真实任务里理解，确认适用边界。
让我们通过一个具体的智能制造案例来整合这些概念。假设你要构建一个刀具磨损预测模型，数据包含机床的振动信号、切削参数和刀具磨损度。数据量约为10万条，来自10把刀具。首先，你进行数据分析，发现磨损度分布不均，高磨损样本占10%。于是，你决定采用分层采样并按照刀具ID分组。你使用`StratifiedGroupKFold`（来自scikit-learn）来实现这一需求。然后，你设计了算法，使用XGBoost回归模型，并通过5折交叉验证调参。在系统架构中，你构建了一个数据管道，该管...


**核心要点**：
- 让我们通过一个具体的智能制造案例来整合这些概念。
- 假设你要构建一个刀具磨损预测模型，数据包含机床的振动信号、切削参数和刀具磨损度。
- 数据量约为10万条，来自10把刀具。

**如何应用**：学习时先画流程，再补文字说明，避免只背名词。
**常见误区**：容易漏掉的是边界条件和例外情况。
**自测标准**：我能写出对应的一个变式题。

---


## 🎯 知识盲区专项突破


### 1. 算法设计

当前水平：high，建议优先学习提升。


### 2. 系统架构

当前水平：high，建议优先学习提升。


### 3. 数据分析

当前水平：high，建议优先学习提升。